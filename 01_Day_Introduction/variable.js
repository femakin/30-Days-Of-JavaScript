// Declaring different variables of different data types
let firstName = 'Asabeneh' // first name of a person
let lastName = 'Yetayeh' // last name of a person
let country = 'Finland' // country
let city = 'Helsinki' // capital city
let age = 100 // age in years
let isMarried = true

// Declaring variables with number values
const gravity = 9.81 // earth gravity  in m/s2
const boilingPoint = 100 // water boiling point, temperature in oC
const PI = 3.14 // geometrical constant

// Variables can also be declaring in one line separated by comma
let name = 'Asabeneh', //name of a person
  job = 'teacher',
  live = 'Finland'

  // Declare four variables without assigning values

  let amount, school, president, region

  // Declare four variables with assigned values

  let company = "Google";
  let number_of_staff = 5000;
  let role = "software Engineer"
  let Salary = 700000

  // Declare variables to store your first name, last name, marital status, country and age in multiple lines

  let firstname_ = "Femi";
  let lastname_ = "Akinyemi";
  let maritalstatus_ = "Married";
  let Country_ = "Nigeria";
  let age_ = 18

  // Declare variables to store your first name, last name, marital status, country and age in a single line

  let [firstname__, lastname__, maritalstatus__, country__, age__  ] = ["Femi", "Akinyemi", "Married", "Nigeria", 18]

// Declare two variables myAge and yourAge and assign them initial values and log to the browser console.
let [myage, yourage] = [18, 20];
console.log(myage)